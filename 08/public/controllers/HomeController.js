angular.module('app').controller('HomeController', function($scope) {
    // Define el modelo
    $scope.hoy = new Date();
    $scope.nombre = null;
    $scope.edad = null;

    // IMPLEMENTAR MÉTODO calcularEdad
});
